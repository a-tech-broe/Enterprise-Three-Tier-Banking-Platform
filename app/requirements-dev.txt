-r requirements.txt
# starlette 1.3.1's TestClient requires httpx2 (the httpx 2.x package), not the
# legacy httpx 0.x distribution. Pinning httpx here makes conftest fail to import.
pytest==8.3.4
httpx2==2.7.0
ruff==0.8.6
