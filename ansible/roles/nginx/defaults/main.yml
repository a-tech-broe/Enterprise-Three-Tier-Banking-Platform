---
# Nginx listens on the ALB target port and reverse-proxies to the local app.
nginx_listen_port: 8080
nginx_upstream_host: "127.0.0.1"
nginx_upstream_port: 8081
nginx_health_path: "/health"
nginx_server_name: "_"
nginx_worker_connections: 1024
nginx_client_max_body_size: "10m"
